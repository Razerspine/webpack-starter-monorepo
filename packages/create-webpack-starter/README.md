# create-webpack-starter

Create a modern webpack starter project.

| Flag                | Description        |
| ------------------- | ------------------ |
| `--template <name>` | Template name      |
| `--no-install`      | Skip npm install   |
| `--dry-run`         | Do not write files |


## Usage

```bash
npx create-webpack-starter my-app
